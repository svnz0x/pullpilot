# Define el token de autenticación para la API de Pullpilot. El resto de ajustes se controla desde la interfaz de usuario o la configuración persistente.
PULLPILOT_TOKEN=coloca_tu_token_aqui
# O bien indica la ruta a un fichero montado que contenga el token (por ejemplo desde Docker secrets).
# PULLPILOT_TOKEN_FILE=/run/secrets/pullpilot_token
